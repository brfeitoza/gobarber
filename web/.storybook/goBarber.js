import { create } from '@storybook/theming';
import logo from '../src/assets/logo.svg';

export default create({
  base: 'dark',
  brandTitle: 'GoBarber Theme',
  brandUrl: 'https://gobarber.com.br',
  brandImage: logo,
});
